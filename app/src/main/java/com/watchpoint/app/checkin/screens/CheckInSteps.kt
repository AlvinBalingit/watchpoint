package com.watchpoint.app.checkin.screens

/** Number of steps in the standard daily check-in flow (mood, stress, readiness, tags). */
const val CHECKIN_STEPS = 4

/** Number of steps in High-Demand Mode's shortened flow (stress, readiness only). */
const val CHECKIN_STEPS_HIGH_DEMAND = 2
